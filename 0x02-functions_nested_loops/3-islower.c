#include "main.h"

/**
 * _islower -> checks if the letter is lowercase
 * @c: is an integer 1 or 0
 * Return: 1 if lowercase 0 if otherwiser
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
